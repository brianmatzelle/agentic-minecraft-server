# Canonical mod list — the source of truth for what ships on the server.
# One Modrinth project slug per line. NeoForge 1.21.1, server-side-safe only.
#
# How this is used:
#   - scripts/deploy.sh turns this list into MODRINTH_PROJECTS in apps/server/.env
#     and (re)starts the container, where itzg/minecraft-server downloads the mods.
#   - Add mods via PR only (see CLAUDE.md "How to handle a mod request"). A human
#     merges; the deploy step applies it. Never edit the live world directly.
#
# Conventions:
#   - `slug              # title — why / notes` (keep the comment, it documents intent)
#   - Modrinth "required" dependencies are auto-resolved by itzg, but list them
#     explicitly below a `# deps:` note so the list is self-documenting.
#   - Blank lines and # comments are ignored by the deploy script.

# ── mods ───────────────────────────────────────────────────────────────────
cc-tweaked            # CC: Tweaked — programmable in-game computers/turtles (ComputerCraft, maintained fork). NeoForge 1.21.1, latest v1.120.0. No required deps. client_side: REQUIRED — players must install it too.
advancedperipherals   # Advanced Peripherals — CC: Tweaked add-on: extra peripherals/blocks (Player Detector, Environment Detector, Chat Box, Energy/Block/Inventory readers, ME/RS bridges, etc.). NeoForge 1.21.1, latest 1.21.1-0.7.62b. client_side: REQUIRED. deps: cc-tweaked (already listed above). Requested via Discord 2026-06-30.
customdiscs-mod       # Custom Discs — upload MP3/WAV/OGG in-game (/disc upload) and play them in jukeboxes; synced 3D positional audio for everyone in range (server-configurable audible radius, default 64 blocks). NeoForge 1.21.1, latest 1.1.0. No required deps. client_side: REQUIRED — ships in the client pack. Requested via Discord 2026-07-23.
cobblemon:Uz1QF4Md    # Cobblemon — catch & battle Pokémon. NeoForge 1.21.1. PINNED to 1.7.1 (version id Uz1QF4Md) to match the exact build the client modpack ships (apps/client/modrinth.index.json). DO NOT unpin: an unpinned slug resolves to Modrinth "latest" and silently drifts ahead of the pinned client → join rejections (the recurring "wrong Cobblemon version" bug). To bump: update BOTH this version id AND the client pack in the same PR. client_side: REQUIRED — players must install it too. deps: kotlin-for-forge.
kotlin-for-forge:NrSebcsG  # Kotlin for Forge — required dependency of Cobblemon (Kotlin runtime). NeoForge 1.21.1. PINNED to 5.11.0 (version id NrSebcsG) to match the exact build the client pack ships. DO NOT unpin: 5.12.0 has since shipped, so an unpinned slug re-resolves to "latest" on each `up` and drifts ahead of the pinned client → handshake/join rejections. To bump: update BOTH this id AND the client pack in the same PR. client_side/server_side: optional (pulled in for Cobblemon).

# ── performance / diagnostics ────────────────────────────────────────────────
lithium               # Lithium — general game-logic optimizations. NeoForge 1.21.1, latest mc1.21.1-0.15.3-neoforge. No deps. client/server: optional — server-side only here.
ferrite-core          # FerriteCore — reduces memory usage. NeoForge 1.21.1, latest v7.0.3. No deps. client/server: optional — server-side only here.
modernfix             # ModernFix — startup/memory/performance fixes. NeoForge 1.21.1, latest v5.27.14+mc1.21.1. No deps. client/server: optional — server-side only here.
spark                 # spark — performance profiler (diagnose lag/TPS). NeoForge 1.21.1, latest v1.10.124-neoforge-1.21.1. No deps. client/server: optional — server-side only here.
chunky                # Chunky — pre-generates chunks to reduce lag spikes. NeoForge 1.21.1, latest v1.4.23. No deps. client/server: optional — server-side only here.
noisium               # Noisium — faster worldgen. NeoForge 1.21.1, latest v2.3.0+mc1.21-1.21.1. No deps. server_side: REQUIRED, client_side: unsupported (server-only).

# ── Cobblemon add-ons (ATMons-inspired bundle) ───────────────────────────────
# All require Cobblemon (already listed above). Several have beta-channel latest
# builds on Modrinth — relies on MODRINTH_ALLOWED_VERSION_TYPE accepting beta.
# Players need these client-side too — they ship in the one-click client modpack
# (apps/client/starting-cc-client.mrpack). After ANY change to this list,
# regenerate the pack: `node scripts/build-client-mrpack.mjs` (see apps/client/README.md).
cobblemon-mega-showdown:qDIlaSjM  # Cobblemon: Mega Showdown — Mega Evolution, Dynamax, Z-moves. RE-ENABLED 2026-07-15, PINNED to 1.6.1+1.7.1+1.21.1 (id qDIlaSjM) — the last build made FOR Cobblemon 1.7.1, matching our pinned cobblemon 1.7.1 above. (The 2026-06-24 boot crash was mega_showdown 1.8.4, a Cobblemon-1.7.3 build, NPEing on datapack load — HeldItems.reload → receiveHeldItemDataFn null — against a mismatched Cobblemon; this version-matched pair avoids that, but WATCH THE FIRST BOOT for the same NPE.) DO NOT unpin: every newer MSD build targets Cobblemon 1.7.3+. Required by legendary-monuments below. NeoForge 1.21.1 (needs loader >= 21.1.214). client_side: REQUIRED — ships in the client pack. deps: cobblemon, architectury-api (>=13.0.8), accessories (>=1.1.0-beta.52), owo-lib (all listed).
simpletms-tms-and-trs-for-cobblemon:uaKfPMYS  # SimpleTMs — TMs & TRs to teach moves. PINNED 2.3.3 (id uaKfPMYS) to the exact build the client pack ships. NeoForge 1.21.1. client_side: REQUIRED. deps: cobblemon, kotlin-for-forge, architectury-api.
cobbledollars:QSl34Qen  # CobbleDollars — in-game Pokémon currency / shops. PINNED 2.0.0+Beta-5.1 (id QSl34Qen) to the exact build the client pack ships. NeoForge 1.21.1. client_side: REQUIRED. deps: cobblemon, kotlin-for-forge.
cobblepedia:dEMwrap5    # Cobblemon Poképedia (Cobblepedia) — in-game Pokédex. PINNED 0.7.1 (id dEMwrap5) to the exact build the client pack ships. NeoForge 1.21.1. client_side: REQUIRED. deps: cobblemon, patchouli.
cobbreeding:xt8IiPEN    # Cobbreeding — Pokémon breeding (egg moves, IV inheritance, daycare). PINNED 2.2.1 (id xt8IiPEN) to the exact build the client pack ships; it targets Cobblemon 1.7, matching our pinned 1.7.1. NeoForge 1.21.1. client_side: REQUIRED. deps: cobblemon, kotlin-for-forge, architectury-api, cloth-config (latest 2.2.1's Modrinth metadata lists no deps, but it's a Cobblemon 1.7 addon — the standard stack is required; cloth-config added below).
cobblemon-pokenav:yCSofpNb  # Cobblemon PokeNav (Cobblenav) — PokéNav-style device/UI: location screen with nearby-Pokémon tracking, PokéFinder, spawn info. PINNED 2.3.3 (id yCSofpNb) to the exact build the client pack ships; its neoforge.mods.toml requires Cobblemon [1.7.0,), satisfied by our pinned 1.7.1 — DO NOT unpin (future builds may target Cobblemon 1.8; same drift trap as the addons above). NeoForge 1.21.1. client_side: REQUIRED — ships in the client pack. deps: cobblemon. Requested via Discord 2026-06-28, re-requested 2026-07-02.
cobblemon-challenge:1feQg7wz  # Cobblemon Challenge — /challenge <player> [level <1-100>] for flat-level competitive PvP battles (default lvl 50, VGC-style): every pokemon is temporarily set to that level for the battle only — non-permanent, no EXP/EVs gained and held items aren't really consumed. Also /challenge3v3, /challenge1v1, /challengedouble, plus a team-preview/lead-select GUI. PINNED 2.4.0 (id 1feQg7wz) — this build is explicitly "For Cobblemon 1.7", matching our pinned 1.7.1; DO NOT unpin: newer builds may target Cobblemon 1.8 and crash during battles (same version-drift trap noted for the addons above). NeoForge 1.21.1. server_side: REQUIRED, client_side: unsupported — fully server-side, vanilla Cobblemon clients need no extra download (NOT in the client pack). No deps (Cobblemon already listed above). Requested via Discord 2026-06-30.
legendary-monuments:l6PiCdpy  # Cobblemon: Legendary Monuments — immersive summoning structures/mechanics for Legendary & Mythical Pokémon (footprints, urns, shrines, Distortion World, Arc Phone). PINNED 8.0.3 (id l6PiCdpy), latest NeoForge build; its neoforge.mods.toml requires cobblemon [1.7.0,) — satisfied by our pinned 1.7.1 — but future builds may target newer Cobblemon/MSD, so DO NOT unpin (same drift trap as the addons above). NeoForge 1.21.1 (needs loader >= 21.1.80). client_side: REQUIRED — ships in the client pack. deps: cobblemon, cobblemon-mega-showdown (above), chipped, lithostitched, accessories (>=1.1.0-beta.52) — all listed. NOTE: 8.0.3 is tuned against MSD 1.8.x; with our pinned MSD 1.6.1 a few recipes referencing newer MSD items may be missing — cosmetic, not boot-breaking (dep range on mega_showdown is [0,)). Requested via Discord 2026-07-15.
cobblemon-secret-base-trainer:tutdY3Ij  # Cobblemon Secret Base Trainer — player-built, fully customizable trainer NPCs ("battle clones"): configurable team/levels, name, player skin (local skin files; the URL-download path was removed in 1.12.27), dialogue lines with optional .ogg voice clips, rewards, win-streak/rematch rules and per-clone cooldowns. Inspired by Emerald's Secret Bases; good for player-run gyms. PINNED 1.14.29 (id tutdY3Ij), latest NeoForge build; its neoforge.mods.toml requires cobblemon [1.6,) — satisfied by our pinned 1.7.1 — but DO NOT unpin: future builds may target Cobblemon 1.8 (same drift trap as the addons above). NeoForge 1.21.1 (needs loader >= 21.1). client_side: REQUIRED — ships in the client pack. deps: cobblemon, rctapi (>=0.14.6-beta, re-added below). Requested via Discord 2026-07-21.
# Cobblemon-addon dependencies:
# rctapi RE-ADDED 2026-07-21 — now a required dep of cobblemon-secret-base-trainer
#   (declared side=BOTH, so it ships in the client pack too). rctmod (Radical
#   Cobblemon Trainers, the 1500-trainer content pack) stays REMOVED — it was
#   removed by request 2026-06-25 and nothing depends on it; rctapi is the
#   standalone trainer/battle API library and runs fine without it.
#   architectury-api stays — required by SimpleTMs, Mega Showdown and now rctapi.
rctapi:zpphgptV       # Radical Cobblemon Trainers API — trainer management & battle API for Cobblemon; required by cobblemon-secret-base-trainer above. PINNED 0.15.2-beta (id zpphgptV), latest NeoForge build (released 2026-04-19, i.e. BEFORE the pinned SBT 1.14.29 was built against it); its neoforge.mods.toml requires cobblemon [1.7,) — satisfied by our pinned 1.7.1 — so DO NOT unpin (future builds may target Cobblemon 1.8). NeoForge 1.21.1. client_side: REQUIRED (SBT declares the dep side=BOTH — ships in the client pack). deps: cobblemon, architectury-api (>=13.0.2, already listed).
# REMOVED 2026-06-23 — boot-fatal against the shipped Cobblemon 1.7.3. Their only
# NeoForge-1.21.1 builds target a different Cobblemon version, so itzg installed them and
# NeoForge refused to load. Re-add when we bump to Cobblemon 1.8.x.
#   cobblemon-ride-on    — v0.3.3 is incompatible with Cobblemon 1.7+ (conflicts with the new
#                          built-in riding system added in 1.7). No 1.7.3-compatible build.
#   cobblemon-counter    — requires cobblemon-tim-core, which needs Cobblemon 1.8.0-SNAPSHOT.
#   cobblemon-tim-core   — (dep of counter) requires Cobblemon 1.8.0-SNAPSHOT; we ship 1.7.3.

# ── content & quality-of-life ────────────────────────────────────────────────
create                # Create — mechanical contraptions & automation. NeoForge 1.21.1, latest 6.0.10+mc1.21.1. server_side: REQUIRED, client_side: optional. No deps.
jade                  # Jade — "what am I looking at" tooltip overlay. NeoForge 1.21.1, latest 15.10.5+neoforge. client/server: optional. No deps.
jei                   # Just Enough Items (JEI) — in-game recipe viewer. NeoForge 1.21.1, latest 19.27.0.343. client/server: optional. No deps.
waystones             # Waystones — fast-travel network. NeoForge 1.21.1, latest 21.1.34+neoforge-1.21.1. client_side: REQUIRED. deps: balm.
sophisticated-backpacks:y2W0V4fw  # Sophisticated Backpacks — upgradable backpacks. NeoForge 1.21.1. PINNED to 1.21.1-3.25.65.1955 (version id y2W0V4fw) to match the build the client pack ships. DO NOT unpin: an unpinned slug re-resolves to "latest" on each up/restart and drifts ahead of the pinned client — it did exactly that on 2026-06-30 (3.25.64 -> 3.25.65 on a routine restart). To bump: update BOTH this id AND the client pack pin in the same change. client_side: REQUIRED. deps: sophisticated-core.
farmers-delight       # Farmer's Delight — cooking & farming expansion. NeoForge 1.21.1, latest 1.21.1-1.3.2. client_side: REQUIRED. No deps.
gravestone-mod        # GraveStone Mod (henkelmax) — drops a gravestone holding your inventory when you die, so you can recover items. NeoForge 1.21.1, latest neoforge-1.21.1-1.0.37. client_side: REQUIRED. No required deps (Jade is an optional integration, already listed). Requested via Discord 2026-07-01.
create-aeronautics    # Create: Aeronautics — flyable physics-driven aircraft for Create. NeoForge 1.21.1, latest 1.3.0+mc1.21.1. client_side: REQUIRED, server_side: REQUIRED. deps: create, sable.
when-dungeons-arise   # When Dungeons Arise — large hand-built dungeon/structure worldgen. NeoForge 1.21.1, latest 2.1.68. server_side: REQUIRED, client_side: not needed (vanilla-block structures generated server-side). No deps. Requested via Discord 2026-06-24.
warputils             # WarpUtils — server-side teleport commands: /sethome+/home, /tpa, /warp, /back (configurable cooldowns/warmups). NeoForge 1.21.1, latest 0.5.3. server_side: REQUIRED, client_side: unsupported — fully server-side, vanilla clients need no extra download (NOT in the client pack). No deps. Requested via Discord 2026-06-28.
cobblemonextrastructures  # Cobblemon: Extra Structures — adds Cobblemon-themed structures + Pokémon spawners (more places to find & catch Pokémon). NeoForge 1.21.1, latest 1.21.1-1.3.0 (changelog states Cobblemon 1.7 compatibility). server_side: REQUIRED, client_side: optional (custom structure content — ships in the client pack as a deselectable optional mod). deps: cobblemon. Requested via Discord 2026-06-28.
vics-point-blank      # Vic's Point Blank — modern 3D guns (rifles, pistols, SMGs, attachments/scopes) with full first-person animations. NeoForge 1.21.1, latest 1.11.1 (release, 2026-03-05). client_side: REQUIRED, server_side: REQUIRED — ships in the client pack. deps: geckolib (below). Requested via Discord 2026-07-18.
superb-warfare        # Superb Warfare — drivable military vehicles: TANKS, helicopters, artillery, drones + modern weapons. NeoForge 1.21.1, latest 0.8.9-final (release, 2026-05-17). client_side: REQUIRED, server_side: REQUIRED — ships in the client pack. deps: geckolib (below), curios (below), kotlin-for-forge (already listed, pinned 5.11.0). Bundles simplebedrockmodel inside the jar (nothing to add). Declares geckoanimfix INCOMPATIBLE — we don't run it anywhere. Requested via Discord 2026-07-21.
aperture-innovations  # Aperture Innovations - Portal Gun — the Portal 1/2 portal gun (blue/orange portals, propulsion/repulsion gels, and more). NeoForge 1.21.1, latest 2.1.1. client_side: REQUIRED, server_side: REQUIRED — ships in the client pack. deps: geckolib (below, already listed). Requested via Discord 2026-07-24.
worldedit             # WorldEdit — in-game terrain/build editor via commands (//wand, //set, //copy, //paste, //undo, brushes, etc.). NeoForge 1.21.1, latest 7.3.8. server_side: REQUIRED, client_side: not needed — runs fully server-side, players use it through commands, so vanilla clients need no extra download (NOT in the client pack). No deps. Note: powerful world-editing — normally restrict to ops/trusted players via permissions. Requested via Discord 2026-07-24.
# QoL dependencies:
balm                  # Balm — required by Waystones (shared config/util lib). NeoForge 1.21.1, latest 21.0.59+neoforge-1.21.1. client/server: optional.
sophisticated-core:IAisFkLf  # Sophisticated Core — required by Sophisticated Backpacks. NeoForge 1.21.1. PINNED to 1.21.1-1.4.60.2057 (version id IAisFkLf) to match the exact build the client pack ships. DO NOT unpin: 1.4.61.x has since shipped, so an unpinned slug re-resolves to "latest" on each `up` and drifts ahead of the pinned client. To bump: update BOTH this id AND the client pack in the same PR. client_side: REQUIRED.
sable                 # Sable — required by Create: Aeronautics (physics/aero library). NeoForge 1.21.1, latest 2.0.3+mc1.21.1. client_side: REQUIRED, server_side: REQUIRED. No required deps (create optional).
geckolib              # GeckoLib — 3D animation engine library, required by Vic's Point Blank (gun models/animations), Superb Warfare, and Aperture Innovations (portal gun). NeoForge 1.21.1, latest 4.9.2. client_side: REQUIRED, server_side: optional per Modrinth but needed here as those mods' required dep. No deps.
curios                # Curios API — accessory-slot library, required by Superb Warfare. NeoForge 1.21.1, latest 9.5.1+1.21.1. client_side: REQUIRED, server_side: REQUIRED. No deps.

# ── Tech, storage & villager QoL — requested via Discord 2026-06-28 ──────────
# All are content mods (blocks/items/registry) → REQUIRED client-side, so they
# also ship in the one-click client pack (CLIENT_MODS in scripts/build-client-mrpack.mjs).
# Core mods listed before their addons; Extra Disks needs Refined Storage.
mekanism              # Mekanism — large tech/automation suite (ore processing, power, factories, logistics). NeoForge 1.21.1, latest 10.7.19.85. client_side: REQUIRED. No required deps.
mekanism-generators   # Mekanism: Generators — power generation addon (solar, gas-burning, wind, fission, fusion). NeoForge 1.21.1, latest 10.7.19.85. client_side: REQUIRED. deps: mekanism.
mekanism-tools        # Mekanism: Tools — tools & armor from Mekanism alloys/materials. NeoForge 1.21.1, latest 10.7.19.85. client_side: REQUIRED. deps: mekanism.
mekanism-additions    # Mekanism: Additions — extra content (Baby Skeletons, plastic blocks, balloons, etc.). NeoForge 1.21.1, latest 10.7.19.85. client_side: REQUIRED. deps: mekanism.
refined-storage       # Refined Storage — networked digital item/fluid storage (RS 2.0 rewrite). NeoForge 1.21.1, latest 2.0.9. client_side: REQUIRED. No required deps.
extra-disks           # Extra Disks — higher-capacity storage disks for Refined Storage. NeoForge 1.21.1, latest 1.21.1-4.0.15 (targets RS 2.0). client_side: REQUIRED. deps: refined-storage.
refined-storage-jei-integration  # Refined Storage - JEI Integration — official Refined Mods addon: JEI recipe transfer (click + to fill RS crafting/pattern grids) + ghost-ingredient drag into filters. Replaces the Forge-1.19.2-only CurseForge "Refined Storage JEI Fix" that was requested. NeoForge 1.21.1, latest 1.0.0. deps: refined-storage, jei (both already listed). client_side: REQUIRED per Modrinth, but ships as OPTIONAL in the client pack tied to JEI (hard-requires JEI, which is optional there; jar is pure JEI-plugin code — no network payloads/registries — so clients without it still join fine). Requested via Discord 2026-07-14.
pylons                # Pylons — area-effect pylons (harvest, growth, item pickup, mob/player effects). NeoForge 1.21.1, latest 5.4.2. client_side: REQUIRED. No deps.
easy-villagers        # Easy Villagers — pick up/move villagers as items + auto-trader/converter/incubator blocks. NeoForge 1.21.1, latest neoforge-1.21.1-1.1.42. client_side: REQUIRED. No deps.
easy-piglins          # Easy Piglins — pick up/move piglins + automatable gold bartering. NeoForge 1.21.1, latest neoforge-1.21.1-1.1.0. client_side: REQUIRED. No deps.
trade-cycling         # Trade Cycling — re-roll/cycle a villager's trade offers. NeoForge 1.21.1, latest neoforge-1.21.1-1.0.18. client_side: REQUIRED. No deps.

# ── Industrial Upgrade tech suite — REMOVED 2026-06-26 by request ─────────────
# Removed the whole IU set: industrialupgrade + its three addons (power-utilities,
# simply-quarries, quantum-generators). They mutually required each other and
# nothing else depended on them, so all four came out together cleanly. Also
# dropped from CLIENT_MODS in scripts/build-client-mrpack.mjs and the client pack
# regenerated in the same PR. Re-add as one set if wanted again.

# ── shared libraries (pulled in by the Cobblemon add-ons above) ───────────────
architectury-api      # Architectury API — required by Mega Showdown, SimpleTMs. NeoForge 1.21.1, latest 13.0.8+neoforge. client_side: REQUIRED.
accessories           # Accessories — required by Cobblemon: Mega Showdown. NeoForge 1.21.1, latest 1.1.0-beta.53+1.21.1. client_side: REQUIRED. deps: owo-lib.
owo-lib               # oωo (owo-lib) — required by Mega Showdown & Accessories. NeoForge 1.21.1, latest 0.12.15.5-beta.1+1.21. client_side: REQUIRED.
patchouli             # Patchouli — in-game documentation lib, required by Cobblepedia. NeoForge 1.21.1, latest 1.21.1-93-neoforge. client_side: REQUIRED.
cloth-config          # Cloth Config API — config-screen library, required by Cobbreeding. NeoForge 1.21.1, latest 15.0.140+neoforge. client_side: REQUIRED (Cobbreeding needs it), server_side: optional.
chipped               # Chipped — thousands of decorative block variants; required by Legendary Monuments (its structures are built from Chipped blocks). NeoForge 1.21.1, latest 4.0.2 (stable since 2024-10). client_side: REQUIRED. deps: resourceful-lib (below); athena is declared side=CLIENT in its neoforge.mods.toml, so it is NOT needed here — it ships only in the client pack (athena-ctm).
resourceful-lib       # Resourceful Lib — shared codec/config library, required by Chipped. NeoForge 1.21.1, latest 3.0.12. client_side: REQUIRED. No deps.
lithostitched         # Lithostitched — worldgen library, required by Legendary Monuments (biome/structure injection; LM 8.0.2+ replaced Terrablender with it). NeoForge 1.21.1, latest 1.7.13-neoforge-21.1. server_side: REQUIRED. Modrinth tags client 'unsupported', but LM declares the dep side=BOTH, so clients running LM need the jar too — it ships in the client pack. No deps.
